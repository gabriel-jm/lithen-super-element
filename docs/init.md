# Lithen - SuperElement - init method

The `init` is a custom method called when SuperElement constructor is called, and after the `render` and `styling` methods.

## Parameters
none

## Usage
```ts
class AppElement extends SuperElement {
  init() {
    // some logic...
  }
}
```
