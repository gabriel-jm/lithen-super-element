# Lithen - SuperElement - render method

The `render` method must return a string or a node element to define the components inner content.

## Parameters
none

## Usage
```ts
class AppElement extends SuperElement {
  render() {
    return '<p>Element using Lithen</p>'
  }
}
```
